import FormLayout from "../Layouts/formLayout";
import { Form } from "./Form";

const FormIndex = () => {
  return (
    <FormLayout>
      <Form />
    </FormLayout>
  );
};

export default FormIndex;
